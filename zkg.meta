[package]
description = Multicast DNS (mDNS) Protocol Analyzer for Zeek IDS for Zeek
credits = François De Keersmaeker <francois.dekeersmaeker@uclouvain.be>
tags = IDS, Zeek, mDNS

test_command = cd testing && make

build_command = ./configure && make
script_dir = ./scripts
