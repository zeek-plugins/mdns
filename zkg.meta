[package]
credits = François De Keersmaeker <francois.dekeersmaeker@uclouvain.be>
tags = mdns, zeek, spicy, ids
script_dir = scripts
build_command = ./configure && cd build && make
test_command = cd testing && btest -c btest.cfg
summary = mDNS plugin for Zeek IDS
description = A mDNS plugin for Zeek IDS

[template]
source = https://github.com/zeek/package-template
version = v1.0.0
zkg_version = 2.13.0
features = plugin

[template_vars]
name = mDNS
namespace = IoT
